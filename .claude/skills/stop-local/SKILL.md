---
description: Stop the local test validator. Use when asked to "stop the validator", "kill the validator", or "stop local".
---

Run the stop script:

```bash
bash .claude/skills/stop-local/scripts/stop.sh
```

Kills the running `solana-test-validator` process. Report whether it was stopped or was not running.
